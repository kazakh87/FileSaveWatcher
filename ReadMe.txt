*TODO*

Create file "SaveFileWatcherConfig" in the project dir and add rows like 'event=onSaved;filter=*.cs;sync=1;notify=1;cmd=copy "{file_path}" "c:\temp"' to copy saved .cs file to temp dir on c. 
